<p id="notice"><%= notice %></p>

<h1>Listing Detours</h1>

<table class="table table-bordered table-hover">
  <thead>
    <tr>
      <th>Adı</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @detours.each do |detour| %>
      <tr>
        <td><%= "#{detour.region_name} / #{detour.forestry.name} / #{detour.name}" %></td>
        <td><%= link_to 'Edit', edit_admin_detour_path(detour) %></td>
        <td><%= link_to 'Destroy', admin_detour_path(detour), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Detour', new_admin_detour_path, class: "btn btn-success" %>
